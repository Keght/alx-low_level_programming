# include <stdio.h>

/**
 * main - The program intrance
 * @void: get's no parameter
 * Return: returns integer 0 (error) or otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
